{"app.js":["ed0728ffe20f2387f8faaeaf3ccc0fe99f3a85886fb38141bcbf5454dd1e30ed","67a0429f683e5ff1409324208aa406571f884e5193c5d42cfb8f7e943d3d1437",[[229,229,"import {drawPlot,plotPlan} from './lanes-ui.js';\n"],[11521,11687,"const linked=(a,b)=>a>=0&&b>=0&&a<W&&b<H&&s.tiles[b*W+a].path;rect(tc,px+3,py+3,10,10,'#a4aa92');rect(tc,px+4,py+4,8,8,'#c8cbb1');if(linked(x-1,y))rect(tc,px,py+4,8,8,'#c8cbb1');if(linked(x+1,y))rect(tc,px+8,py+4,8,8,'#c8cbb1');if(linked(x,y-1))rect(tc,px+4,py,8,8,'#c8cbb1');if(linked(x,y+1))rect(tc,px+4,py+8,8,8,'#c8cbb1');rect(tc,px+5,py+6,6,1,'#a6ad98');rect(tc,px+7,py+4,1,7,'#e0dfc5"],[17325,17679,"drawPlot(ctx,s,selectedBuild,hover,qolUI?.movingId??null);if(selectedBuild!=='path'){ctx.globalAlpha=.6;ctx.drawImage(buildingSprite(selectedBuild),hover.x*TILE-8,hover.y*TILE"],[29712,29712,"localStorage.getItem('littlefolk.pre23.save'))localStorage.setItem('littlefolk.pre23.save',lastGoodSave);if(!"],[33822,33823,"3"],[43745,43745,"getView:()=>({selection,selectedBuild}),inspectBuilding:id=>inspect('building',id),"]]],"qol-ui.js":["10e4281d81d49ab53abf7ccdcf7b50080bf6beac98ebf93a1ceea4cc2f209c31","b365031fa722ca8d5f0329a08706c1dc915bb6129a088dd11a7d468dd26ca4b9",[[0,0,"import {plotPlan} from './lanes-ui.js';\n"],[216,216,",planPaving,pave"],[981,981,",paveFrom=null,paveTo=null,pavePlan=null"],[1523,1523,"id=\"laneLegend\" hidden><b class=\"in\">Blue → in</b> · <b class=\"out\">Gold → out</b> · Arrows show deliveries</div><div "],[4096,4096,"    if(b.id==='confirmPaving'&&paveFrom&&paveTo){const r=pave(S(),paveFrom,paveTo);if(r.ok){paveFrom=null;paveTo=null;pavePlan=null;api.changed();api.toast('Paving laid. Faster trips, one tidy stretch.',true);}else api.toast(r.reason);buildKey='';update();return;}\n    if(b.id==='restartPaving'){paveFrom=null;paveTo=null;pavePlan=null;buildKey='';update();return;}\n"],[5635,5635,"paveFrom=null;paveTo=null;pavePlan=null;"],[8761,8761,"    if(api.getView().selectedBuild==='path'){if(!paveFrom){paveFrom={x:x+.5,y:y+.5};paveTo=null;}else paveTo={x:x+.5,y:y+.5};pavePlan=paveTo?planPaving(S(),paveFrom,paveTo):null;buildKey='';renderBuild();return true;}\n"],[12648,12648,"    if(type==='path'){\n      if(paveFrom&&paveTo)pavePlan=planPaving(s,paveFrom,paveTo);\n      const pk=JSON.stringify(['paving',paveFrom,paveTo,pavePlan?.cost,s.stone]);if(pk===buildKey)return;buildKey=pk;\n      $('buildTitle').textContent='Lay a paved walk';$('buildText').textContent='Straight runs, neat corners. Faster folk and wagons.';\n      $('placementTools').innerHTML='<p class=\"taskcopy\">'+(!paveFrom?'Tap where the paving should start.':!paveTo?'Now tap its other end. The route will avoid obstacles.':'Preview the whole stretch, then confirm.')+'</p>'+(pavePlan?'<div class=\"siteFacts\" id=\"paveCost\">'+(pavePlan.path.length?pavePlan.path.length+' tiles · '+pavePlan.cost+' stone (existing paving is free)':pavePlan.reason)+'</div><button class=\"primary wide\" id=\"confirmPaving\" '+(pavePlan.ok?'':'disabled')+'>Lay this paving</button>':'')+(paveFrom?'<button class=\"secondary wide\" id=\"restartPaving\">Choose a new start</button>':'');return;\n    }\n"],[14539,14648,"';}\n    else if(site){const plan=plotPlan(s,type,site.x,site.y,moving);html+='<div class=\"siteLegend\"><span><i class=\"green\"></i>Fits</span><span><i class=\"amber\"></i>Clear first</span><span><i class=\"red\"></i>Blocked</span></div>';if(plan.geometry.ok){html+='<div class=\"siteFacts\">'+(plan.hub?'Loading pad → '+BUILDINGS[plan.hub.type].short+' #'+plan.hub.id+(s.bloom.autoConnect?'. A shared delivery lane will be laid.':'. Nearby storage; automatic links are off.'):'One connected entrance stays open.')+(RECIPES[type]?.power?' Wind estimate: '+Math.round(plan.wind*100)+'%.':'')+(plan.geometry.refund?' '+plan.geometry.refund+' covered paving stones will be returned.':'')+'</div>';}"],[20157,20157,"if(paveFrom){c.strokeStyle='#fae3a0';c.lineWidth=2;c.strokeRect(paveFrom.x*TILE-5,paveFrom.y*TILE-5,10,10);}if(pavePlan?.path?.length){c.strokeStyle=pavePlan.ok?'#f5e0a2':'#d49380';c.lineWidth=5;c.setLineDash([3,2]);c.beginPath();pavePlan.path.forEach((p,j)=>j?c.lineTo(p.x*TILE,p.y*TILE):c.moveTo(p.x*TILE,p.y*TILE));c.stroke();c.setLineDash([]);}"],[21211,21211,"movingId(){return moving;},get "]]],"industry-ui.js":["6f2fc830ddebb20b05ea32b7b77b3c8344a69cecff31ed96627d8bf3931898eb","bac5fea110d898e41f4fd09d159455c47191b0542b1ebbb737ae953305ecdd9e",[[0,0,"import {drawLanes,connectionRows} from './lanes-ui.js';\nimport {laneMetrics,refreshLanes} from './lanes.js';\n"],[275,275,",optimizeStorage,restoreFlowSettings"],[5143,5143,"  const networkButton=document.createElement('button');networkButton.id='networkBtn';networkButton.className='category';networkButton.textContent='⇄ Lanes';networkButton.setAttribute('aria-pressed','false');$('industryBtn').before(networkButton);\n"],[5804,5826,"Village Works · 2.3"],[6471,6472,"3"],[10152,10193,"unallocated product is ready. Ingredients already assigned to other workshops do not count. Supplies set aside for wishes and building are excluded, so a full savings pile cannot starve downstream workshops"],[12614,12614,"      html+='<div class=\"networkGuide\">Packed-earth lanes are built with cargo links. Opposite deliveries share a lane; paved tiles make both faster. Manual routes stay yours.</div><div class=\"smallactions\"><button class=\"primary\" id=\"optimizeStorage\">Re-plan storage links</button><button class=\"secondary\" id=\"restoreFlow\">Restore sensible stock settings</button></div>';\n"],[13135,13135," · ${laneMetrics(s,q).tiles} tiles · ~${laneMetrics(s,q).seconds.toFixed(1)}s one way"],[13617,13617,"id==='networkBtn'){showRoutes=!showRoutes;networkButton.setAttribute('aria-pressed',String(showRoutes));api.toast(showRoutes?'Delivery arrows on. Select a workshop: blue in, gold out.':'Delivery arrows off. The lanes stay visible.');return;}if(b."],[14354,14354,"    if(b.id==='optimizeStorage'){const r=optimizeStorage(s);api.changed();renderBook();api.toast(r.count?r.count+' auto links shortened by '+r.saved+' tiles. Manual links kept.':'Auto storage links already use the nearest reachable loading points. Manual links kept.');return;}\n    if(b.id==='restoreFlow'){restoreFlowSettings(s);api.changed();renderBook();api.toast('Building reserves and production targets restored. Your goods and custom links are unchanged.');return;}\n"],[14488,14511,"{showRoutes=!showRoutes;networkButton.setAttribute('aria-pressed',String(showRoutes));}"],[16358,16358,"    if(b.dataset.inspectLink){api.inspectBuilding(Number(b.dataset.inspectLink));return;}\n"],[18349,18349,"connectionList\" id=\"connectionList\"></div><div class=\""],[21308,21308,"    const rows=connectionRows(s,b.id),list=$('connectionList');if(list){list.hidden=!rows.length;const html=rows.length?'<h4>DELIVERIES · '+rows.length+' connections</h4>'+rows.map(r=>`<button class=\"connectionRow ${r.out?'out':'in'}\" data-inspect-link=\"${r.other}\"><span class=\"arrow\">${r.out?'↑':'↓'}</span><span><b>${r.out?'To':'From'} ${esc(r.label)}</b><small>${esc(r.items)} · ${r.blocked?'Blocked':r.paused?'Paused':r.tiles+' tiles · ~'+r.seconds.toFixed(1)+'s'}</small></span></button>`).join(''):'';if(list._html!==html){list._html=html;list.innerHTML=html;}}\n"],[21362,21362,"const legend=$('laneLegend'),view=api.getView?.();if(legend){legend.hidden=!!document.querySelector('.sheetback:not([hidden])')||!(showRoutes||view?.selection?.kind==='building'&&i.routes.some(r=>r.from===view.selection.id||r.to===view.selection.id));}\n"],[23300,23352,"Delivery lane connected. Arrows show which way the goods travel"],[24066,24808,"drawLanes(c,s,selection,showRoutes);"],[26140,26140,"if(!s.industry.routes.find(r=>r.id===p.route)?.path?.length)continue;"]]]}
