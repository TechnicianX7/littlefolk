{
  "name": "littlefolk",
  "version": "2.3.0",
  "private": true,
  "type": "module",
  "description": "A cozy autonomous pixel-art village for the web and iPad.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "check": "node --check world.js && node --check app.js && node --check sw.js && node --check industry.js && node --check industry-ui.js && node --check qol-ui.js && node --check bloom.js && node --check bloom-ui.js && node --check lanes.js && node --check lanes-ui.js"
  }
}
