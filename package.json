{
  "name": "littlefolk",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A cozy autonomous pixel-art village for the web and iPad.",
  "scripts": {
    "test": "node --test tests/world.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "check": "node --check world.js && node --check app.js && node --check sw.js"
  }
}
