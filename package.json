{
  "name": "littlefolk",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "A cozy autonomous pixel-art village for the web and iPad.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.mjs",
    "check": "node --check world.js && node --check app.js && node --check sw.js && node --check industry.js && node --check industry-ui.js"
  }
}
